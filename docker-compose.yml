services:
  server:
    build: ./server
    network_mode: "host"

  client:
    build: ./client
    depends_on:
      - server
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
    network_mode: "host"